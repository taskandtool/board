# Off-platform only. On Task & Tool every value below arrives through the
# platform and this file is not used.
PORT=3000
# DATABASE_URL=postgres://...                 # any Postgres; the board creates its own tables
# BOARD_USER=you@example.com                  # stands in for the identity header off-platform
